<% if @post.errors.any? %>
  <div>
    The form contains <%= pluralize(@post.errors.count, "error") %>.
  </div>
  <ul>
    <% @post.errors.full_messages.each do |msg| %>
      <li><%= msg %></li>
    <% end %>
  </ul>
<% end %>
